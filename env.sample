# 测试用教务系统账号（复制本文件为 env 并填入真实凭据）
# cp env.sample env

TEST_USERNAME=your_student_id
TEST_PASSWORD=your_password

# 智慧考勤的「智慧山体」统一认证账号（可选）。
# 与教务账号相同时留空即可，集成测试会回退到上面的 TEST_USERNAME / TEST_PASSWORD。
TEST_ATTENDANCE_USERNAME=
TEST_ATTENDANCE_PASSWORD=
